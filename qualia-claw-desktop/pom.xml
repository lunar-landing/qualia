<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>cn.lunarlanding</groupId>
        <artifactId>qualia</artifactId>
        <version>${revision}</version>
    </parent>

    <groupId>cn.lunarlanding</groupId>
    <artifactId>qualia-claw-desktop</artifactId>
    <version>${revision}</version>
    <name>qualia-claw-desktop</name>
    <description>Qualia Claw Desktop - 原生桌面壳，内嵌系统 WebView 加载本地 Web 界面</description>

    <properties>
        <java.version>17</java.version>
        <!-- SWT 版本 3.124+ 支持 Windows Edge(WebView2) 后端与 macOS WKWebView -->
        <swt.version>3.128.0</swt.version>
    </properties>

    <dependencies>
        <!-- qualia-claw：复用 ClawWebApplication / AgentRegistry / 配置与前端静态资源 -->
        <dependency>
            <groupId>cn.lunarlanding</groupId>
            <artifactId>qualia-claw</artifactId>
            <version>${revision}</version>
        </dependency>
        <!-- JNA：调用 Windows DWM 原生接口，给系统标题栏/边框做深浅色（仅 Windows 生效，其他平台运行期不加载） -->
        <dependency>
            <groupId>net.java.dev.jna</groupId>
            <artifactId>jna</artifactId>
            <version>5.14.0</version>
        </dependency>
    </dependencies>

    <!--
      SWT 原生库按操作系统 + 架构区分 artifact，通过 profile 自动激活（os.family / os.arch）。
      平台专用 jar 会声明一个不存在的 * 依赖，需 exclusion 排除，
      否则依赖解析失败（SWT 常见坑）。
    -->
    <profiles>
        <!-- Windows x86_64：Edge(WebView2) 后端 -->
        <profile>
            <id>swt-win-x86_64</id>
            <activation>
                <os>
                    <family>windows</family>
                    <arch>amd64</arch>
                </os>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>org.eclipse.platform</groupId>
                    <artifactId>org.eclipse.swt.win32.win32.x86_64</artifactId>
                    <version>${swt.version}</version>
                    <exclusions>
                        <exclusion>
                            <groupId>org.eclipse.platform</groupId>
                            <artifactId>*</artifactId>
                        </exclusion>
                    </exclusions>
                </dependency>
            </dependencies>
        </profile>

        <!-- macOS Apple Silicon (aarch64)：WKWebView 后端 -->
        <profile>
            <id>swt-mac-aarch64</id>
            <activation>
                <os>
                    <family>mac</family>
                    <arch>aarch64</arch>
                </os>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>org.eclipse.platform</groupId>
                    <artifactId>org.eclipse.swt.cocoa.macosx.aarch64</artifactId>
                    <version>${swt.version}</version>
                    <exclusions>
                        <exclusion>
                            <groupId>org.eclipse.platform</groupId>
                            <artifactId>*</artifactId>
                        </exclusion>
                    </exclusions>
                </dependency>
            </dependencies>
        </profile>

        <!-- macOS Intel (x86_64)：WKWebView 后端 -->
        <profile>
            <id>swt-mac-x86_64</id>
            <activation>
                <os>
                    <family>mac</family>
                    <arch>x86_64</arch>
                </os>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>org.eclipse.platform</groupId>
                    <artifactId>org.eclipse.swt.cocoa.macosx.x86_64</artifactId>
                    <version>${swt.version}</version>
                    <exclusions>
                        <exclusion>
                            <groupId>org.eclipse.platform</groupId>
                            <artifactId>*</artifactId>
                        </exclusion>
                    </exclusions>
                </dependency>
            </dependencies>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>17</source>
                    <target>17</target>
                    <encoding>UTF-8</encoding>
                    <parameters>true</parameters>
                </configuration>
            </plugin>
            <!-- Maven Shade Plugin：生成含全部依赖的可执行 jar，供 jpackage 打包 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <!-- 剥除依赖（如签名的 SWT jar）的签名文件，否则 uber-jar 会因签名摘要不匹配报 SecurityException -->
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>cn.lunarlanding.qualia.claw.desktop.ClawDesktopLauncher</mainClass>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
